{
  "name": "private-mailbox",
  "version": "0.0.0",
  "private": true,
  "description": "Self-hosted single-user webmail client for contact@arnabray.me",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev:server": "pnpm --filter server dev",
    "dev:web": "pnpm --filter web dev",
    "imap-check": "pnpm --filter server imap-check",
    "build": "pnpm --filter server build && pnpm --filter web build"
  }
}
